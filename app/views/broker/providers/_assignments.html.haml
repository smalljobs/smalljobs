- if @provider.assignments.count > 0
  %div.provider_allocations
    %h4.assignments-header= 'Einsätze'
    %ul.allocations-list
      - @provider.assignments.order(created_at: :desc).each do |assignment|
        %li.allocations-list-item
          %p
            = link_to "#{assignment.seeker.name}, #{assignment.description}", edit_broker_seeker_path(assignment.seeker)
