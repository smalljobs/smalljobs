%div
  %label
    = "Standorte"
  %div
    = link_to "Neuen Standort", '#', class: "btn btn-success", data: {toggle: "modal",
        target: "#organization_new_modal", url: broker_region_organizations_path(format: :json)}
  %div.tab-content
    %div{id: 'todo', class: 'tab-pane fade in active'}
      .table-responsive.table-margin
        %table.table.table-striped.sortable
          %thead
            %tr
              %th.sortable{id: 'organization_name'} name
              %th.sortable{id: 'organization_strasse'}  Strasse, Nr
              %th.sortable{id: 'organization_plz'} PLZ Ort
          %tbody.list
            - if @organizations.size == 0
              %tr
                %td{colspan: 3}= "Es sind noch keine Todo in deiner Region erfasst"
            - @organizations.each do |organization|
              %tr
                %td.modal-link
                  = link_to edit_broker_region_organization_path(organization) do
                    = organization.name
                %td
                  = organization.street
                %td
                  = "#{organization&.place&.zip} #{organization&.place&.province}"