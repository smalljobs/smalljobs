- if @job.assignments.count > 0
  %div.bg-info.provider_allocations
    %h4= 'Einsätze'
    %ul.allocations-list
      - for assignment in @job.assignments
        %li.allocations-list-item
          = link_to "#{assignment.provider.name}, #{assignment.description}", edit_broker_seeker_path(assignment.seeker)