%h1= t('broker.jobs.edit.title')
%p.lead= t('broker.jobs.edit.lead')

= render 'form'

%ul.nav.nav-pills
  %li= link_to t('common.show'), broker_job_path(@job)
  %li= link_to t('common.back'), broker_jobs_path
