%p Willkommen #{ @resource.firstname } #{ @resource.lastname }!
%p Sie können Ihre Benutzerkonto mit dem folgenden Link bestätigen:
%p= link_to 'Email bestätigen', confirmation_url(@resource, confirmation_token: @token, subdomain: @resource.subdomains.first)
