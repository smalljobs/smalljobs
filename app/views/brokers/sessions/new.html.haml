%h2= t('brokers.sessions.title')
%p.lead= t('brokers.sessions.lead')

= simple_form_for(resource, as: resource_name, url: session_path(resource_name), html: { novalidate: true }) do |f|

  .form-inputs
    = f.input :email, required: false, autofocus: true
    = f.input :password, required: false
    = f.input :remember_me, as: :boolean if devise_mapping.rememberable?

  .form-actions
    = f.button :submit, t('brokers.sessions.action'), class: 'btn btn-primary'

= render 'devise/shared/links'
